# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
Readme.local.*

# Dependencies
node_modules

# Build output
dist
dist-ssr

# Cloudflare
.wrangler
.dev.vars

# Environment
.env
.env.*
!.env.example
*.local

# Large media (too big for Cloudflare Workers 25 MiB asset limit)
videos/
*.mp4
*.mov
*.avi
*.mkv

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Project-specific
.claude/hooks/.state
closfa.zip
/to\ read/

# Vendored skill source — kept locally for authoring, never shipped to git or
# Cloudflare. It is a third-party plugin repo, not part of the app.
superpowers-main/

# Throwaway probe scripts. These historically carried live DB/ImageKit
# credentials; keep them out of the repo permanently.
check_post.mjs
test_*.mjs
test_*.avif

# Temporary Vite config bundles
app.config.timestamp_*
